# SORNET Admin Mobile App (Flutter)

This directory contains the complete source code for the **SORNET Admin Mobile Application**.

## Build & Run

```bash
# Get dependencies
flutter pub get

# Check analyzer
dart analyze

# Run unit and widget tests
flutter test

# Run in debug mode on connected device / emulator
flutter run

# Build release APK
flutter build apk --release

# Build debug APK
flutter build apk --debug
```

## Generated APK Outputs
- Release APK: `build/app/outputs/flutter-apk/app-release.apk`
- Debug APK: `build/app/outputs/flutter-apk/app-debug.apk`
- Release Folder: `../release_apks/sornet-admin-release.apk`

## Default Admin Credentials
- **Email:** `admin@sornet.com`
- **Password:** `Admin@123`
